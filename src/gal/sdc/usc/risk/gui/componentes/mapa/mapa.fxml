<?xml version="1.0" encoding="UTF-8"?>

<?import com.jfoenix.controls.JFXButton?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>
<?import java.lang.Float?>
<?import java.net.URL?>
<AnchorPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml"
            fx:controller="gal.sdc.usc.risk.gui.componentes.mapa.MapaController"
            maxWidth="Infinity" maxHeight="Infinity">
    <stylesheets>
        <URL value="@mapa.css"/>
    </stylesheets>
    <fx:define>
        <Float fx:id="columnas" fx:value="11"/>
        <Float fx:id="filas" fx:value="8"/>
        <Float fx:id="wMargen" fx:value="20"/>
        <Float fx:id="hMargen" fx:value="15"/>
    </fx:define>

    <VBox fx:id="contenedor" AnchorPane.leftAnchor="0.0" AnchorPane.rightAnchor="0.0" AnchorPane.topAnchor="0.0"
          AnchorPane.bottomAnchor="0.0" styleClass="oceano">
        <HBox prefHeight="${(contenedor.height - hMargen) / filas}">
            <JFXButton id="0-0" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="0-1" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="0-2" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="0-3" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="0-4" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="0-5" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="0-6" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="0-7" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="0-8" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="0-9" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="0-10" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
        </HBox>
        <HBox>
            <JFXButton id="1-0" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="1-1" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="1-2" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="1-3" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="1-4" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="1-5" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="1-6" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="1-7" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="1-8" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="1-9" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="1-10" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
        </HBox>
        <HBox>
            <JFXButton id="2-0" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="2-1" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="2-2" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="2-3" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="2-4" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="2-5" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="2-6" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="2-7" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="2-8" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="2-9" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="2-10" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
        </HBox>
        <HBox>
            <JFXButton id="3-0" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="3-1" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="3-2" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="3-3" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="3-4" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="3-5" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="3-6" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="3-7" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="3-8" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="3-9" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="3-10" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
        </HBox>
        <HBox>
            <JFXButton id="4-0" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="4-1" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="4-2" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="4-3" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="4-4" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="4-5" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="4-6" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="4-7" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="4-8" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="4-9" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="4-10" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
        </HBox>
        <HBox>
            <JFXButton id="5-0" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="5-1" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="5-2" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="5-3" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="5-4" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="5-5" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="5-6" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="5-7" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="5-8" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="5-9" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="5-10" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
        </HBox>
        <HBox>
            <JFXButton id="6-0" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="6-1" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="6-2" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="6-3" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="6-4" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="6-5" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="6-6" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="6-7" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="6-8" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="6-9" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="6-10" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
        </HBox>
        <HBox>
            <JFXButton id="7-0" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="7-1" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="7-2" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="7-3" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="7-4" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="7-5" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="7-6" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="7-7" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="7-8" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="7-9" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
            <JFXButton id="7-10" prefWidth="${(contenedor.width - wMargen) / columnas}"
                       prefHeight="${(contenedor.height - hMargen) / filas}"
                       HBox.hgrow="ALWAYS"/>
        </HBox>
    </VBox>
</AnchorPane>
